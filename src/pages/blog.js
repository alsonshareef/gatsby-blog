import React from 'react';

import Layout from '../components/Layout/Layout';

const Blog = () => {
    return (
        <div>
            <Layout>
                <h1>Blog</h1>
            </Layout>
        </div>
    );
};

export default Blog;
